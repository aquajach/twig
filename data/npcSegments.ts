import { andy } from '@/data/contextSegments/andy';
import { dev } from '@/data/contextSegments/dev';
import { manager } from '@/data/contextSegments/manager';

export const npcSegments = {
  manager,
  dev,
  andy,
} as const;
